[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisord.pid

[program:php-fpm]
command=php-fpm -F
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
autorestart=true
startretries=0

[program:nginx]
command=nginx -g 'daemon off;'
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
autorestart=true
startretries=0

[program:vite]
command=npm run dev -- --host 0.0.0.0
directory=/var/www/html
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
autorestart=true
startretries=0

# Optional: Laravel Queue Worker (sudah dihandle oleh docker-compose.yml)
# Uncomment jika ingin queue worker di dalam container app
# [program:laravel-worker]
# process_name=%(program_name)s_%(process_num)02d
# command=php /var/www/html/artisan queue:work --sleep=3 --tries=3 --max-time=3600
# directory=/var/www/html
# autostart=true
# autorestart=true
# stopasgroup=true
# killasgroup=true
# user=www-data
# numprocs=1
# redirect_stderr=true
# stdout_logfile=/var/log/supervisor/worker.log
# stopwaitsecs=3600

# Optional: Laravel Scheduler (sudah dihandle oleh docker-compose.yml)
# Uncomment jika ingin scheduler di dalam container app
# [program:laravel-schedule]
# command=php /var/www/html/artisan schedule:work
# directory=/var/www/html
# autostart=true
# autorestart=true
# stopasgroup=true
# killasgroup=true
# user=www-data
# redirect_stderr=true
# stdout_logfile=/var/log/supervisor/schedule.log

